/// A small fixed spacing scale so gaps between elements read as deliberate
/// rhythm rather than ad hoc SizedBox values scattered per screen.
abstract class AppSpacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 32.0;
}
